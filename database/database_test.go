package database

import (
    "testing"
)

func TestConnect(t *testing.T) {
    // Given
    // mock gorm.DB
    // mock gorm.Open("postgres", dsn)

    // When
    Connect()

    // Then
    // assert database connection is successful
}